#include <stdio.h>
#include <time.h>

int get_cpu_speed() {
  // TODO: this function should return the clock speed of the  
  // CPU in MHz
  return 0;
}

int main() {
  printf("CPU clock has %d MHz\n", get_cpu_speed());
  return 0;
}

